<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Payroll</title>
</head>
<body>
    <%- include("header") %>

    <% if(allEmployees) { %>
        <form action="/payroll" method="post">
            <label for="employee">Selecciona un empleado</label>
            <select name="employeeId" id="employee">
                <% allEmployees.forEach((employee) => { %>
                    <option value="<%= employee.id %>"><%= employee.nombre_completo %></option>
                <% }) %>                
            </select>

            <button type="submit">Calcular liquidacion</button>
        </form>
    <% } %>

    <% if(employee) { %>
        <h1>Rubros remunerativos</h1>
        <p>Salario basico: $ <%= employee.salario_basico %></p>
        <p>Antiguedad: $ <%= employee.valorPorAntiguedad %></p>
        <% if(employee.valorPorSecundario !== 0) { %>
            <p>Titulo secundario: $ <%= employee.valorPorSecundario %></p> 
        <% } %>
        <p>Total: $ <%= employee.salarioTotal %></p>

        <br>

        <h1>Aportes</h1>
        <p>SIPA Ley 24.241: $ <%= employee.contribuciones.aporteJubilacion[0] %></p>
        <p>INSSJyP Ley 19.032: $ <%= employee.contribuciones.aporteJubilacion[1] %></p>
        <p>Obra Social Ley 23.660: $ <%= employee.contribuciones.obraSocial %></p>
        <p>Cuota Sindical 2%: $ <%= employee.contribuciones.aporteSindical %></p>

        <br>

        <h1>Salario final: $ <%= employee.contribuciones.salarioFinal %></h1>
    <% } %>
</body>
</html>